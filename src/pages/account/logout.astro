---
import { ENV } from "../../utils/env.util";
import { ROUTES } from "../../utils/routes.const";

const response = Astro.redirect(ROUTES.login);
response.headers.append("Set-Cookie", `${ENV.AUTH_COOKIE_TOKEN}=''`);
return response;
---
